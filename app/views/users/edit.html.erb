<%= bootstrap_form_for :user, url: user_path, method: 'put' do |f| %>
  <%= f.text_field :name %>
  <%= f.email_field :email %>
  <%= f.submit 'Update' %>
  <%= link_to 'Change Password', password_user_path, class: 'btn btn-default' %>
<% end %>