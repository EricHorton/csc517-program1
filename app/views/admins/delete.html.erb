<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>

<div class="admins delete">
  <h2>Delete Admin</h2>

  <%= form_for(:subject, :url => {:action => 'destroy', :id => @admin.id}) do|f| %>
      <% if @admin.email == 'admin@portal.net' || @admin.id == session[:user_id]%>
          <div class="one"><p>You cannot delete this admin.</p></div>
      <% else %>
          <div class="one"><p>Are you sure you want to delete this admin?</p></div>
          <div class="form-buttons">
            <%= submit_tag("Delete") %>
          </div>
      <% end %>



      <%end%>
</div>